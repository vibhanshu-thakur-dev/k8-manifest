apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kong-admin
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  ingressClassName: traefik
  rules:
  - host: kong-admin.home-network.sort-tree.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: kong-kong-admin
            port:
              number: 8001