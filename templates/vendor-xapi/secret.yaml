apiVersion: v1
kind: Secret
metadata:
  name: vendor-xapi-secrets
  namespace: integrations
  labels:
    app: vendor-xapi
type: Opaque
# Values here are plain-text placeholders. In production use Sealed Secrets,
# External Secrets Operator, or your CI/CD pipeline to inject real values —
# never commit real secrets to source control.
stringData:
  ACTIVEMQ_PASSWORD: "admin"
  SHOPIFY_SIGNING_SECRET: "changeme"
  GOCARDLESS_SIGNING_SECRET: "changeme"
